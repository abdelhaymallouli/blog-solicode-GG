## 🔹 Fonctions pour la page Accueil

- `getPublicArticles(array $filters)` — Récupère les articles visibles par les visiteurs avec filtres  
- `getArticleBySlug(string $slug)` — Récupère un article spécifique par son slug  
- `getRecentArticles(int $limit)` — Récupère les articles récents pour l’affichage sur l’accueil  
- `searchArticles(string $query)` — Recherche dynamique d’articles  
- `getCategories()` — Liste toutes les catégories disponibles  
- `getTags()` — Liste tous les tags disponibles  
- `getAuthorInfo(int $userId)` — Récupère les informations de l’auteur  

